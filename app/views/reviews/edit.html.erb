<H1>Edit a Review</H1>

<%= simple_form_for [@experience, @review] do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating %>
  <%= f.submit "Edit Review", class: "btn btn-primary"%>
<% end %>

<%= link_to "Go back", experience_path(@experience) %>
