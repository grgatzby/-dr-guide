<% image_tag = "gqwk5xhntgi8nsxajvi6" %>
<div class="image-front"
  style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path image_tag, height: 500, crop: :fill %>')"
  >
  <h1>Find thousands of experiences all around the world.</h1>
</div>

  <div class="cards-experiences">
  <% @experiences.each do |experience|  %>
    <%= render "experience", experience: experience %>
  <% end %>
  <div>
    <%= "simple_form_for [@experiences] do |f|"%>
  </div>
  <div>


  </div>
</div>
